# cc-cost

Claude Code のセッションログを集計し、モデル単価と為替レートから利用コスト(USD/JPY)を算出して表示するCLIツール。

## Language

**ログルート (Log root)**:
`<ログルート>/<project-dir>/<session-uuid>.jsonl` の形でセッションログを持つ、スキャン対象のディレクトリ。デフォルトは `$CLAUDE_CONFIG_DIR/projects`(未設定なら `$HOME/.claude/projects`)で、設定ファイルの `roots` で追加のログルートを加算できる。異なる環境(マシン、シェル)で `CLAUDE_CONFIG_DIR` を使い分けているユーザーが、複数のログルートを1回の実行でまとめて集計するために存在する。
_Avoid_: ルートディレクトリ、projectsディレクトリ

**セッション (Session)**:
`<ログルート>/<project-dir>/<session-uuid>.jsonl` の1ファイルに対応する、1回のClaude Codeでのやり取りのまとまり。
_Avoid_: ログファイル、会話

**ログエントリ (Log entry)**:
セッションファイルの1行。assistantメッセージのログエントリは `model` と `usage` を持つ。1件のログエントリは必ずしも1件のAPI呼び出しと1対1ではない(同一のAPI呼び出しに対して、thinkingブロックとtool_useブロックなど複数のログエントリが出力され、同じ`usage`を共有することがある)。
_Avoid_: 行、レコード

**API呼び出し (API call)**:
Anthropic APIへの1回のリクエスト・レスポンス。課金の単位はこれであり、ログエントリではない。同一のAPI呼び出しに属するログエントリは `requestId`(またはassistantメッセージの`message.id`)で識別できる。
_Avoid_: リクエスト、呼び出し

**使用量 (Usage)**:
1回のAPI呼び出しにおけるトークン数の内訳。入力(input)、出力(output)、キャッシュ書込(5分TTL / 1時間TTL)、キャッシュ読込(cache read)の5種類に分かれる。
_Avoid_: トークン数、利用量

**サイドチェーン (Sidechain)**:
ログエントリの `isSidechain: true` が示す、サブエージェント呼び出しなどメインの会話の脇で発生したAPI呼び出し。実際に課金が発生するため、コスト集計に含める。
_Avoid_: サブエージェント呼び出し(コード上の実装概念と紛らわしいため、集計対象としてはサイドチェーンで統一する)

**プロジェクト (Project)**:
ログルート配下の1ディレクトリ。ディレクトリ名は元のカレントディレクトリのパスの `/` を `-` に変換したもの。異なるログルートに同名のディレクトリが存在しても、常に別プロジェクトとして扱い、usageは合算しない。
_Avoid_: ワークスペース、リポジトリ

**プロジェクトグループ (Project group)**:
設定ファイルの正規表現パターン(プロジェクトのディレクトリ名に対してマッチする)でプロジェクトをまとめた、表示上の集計単位。パターンに定義順でマッチした最初のグループに属する。同名のディレクトリが異なるログルートにまたがってパターンにマッチした場合も、同じグループへ合算される。どのパターンにもマッチしないプロジェクトは、ディレクトリ名を読みやすいパス形式に変換したもの自体が1つのプロジェクトグループになる(この場合のみ、ログルートをまたいだ名前の衝突があればグループ名にログルートの識別情報が付与され、別グループとして区別される)。
_Avoid_: カテゴリ、タグ

**単価テーブル (Pricing table)**:
モデルごとの、使用量の各項目(入力/出力/キャッシュ書込5分/キャッシュ書込1時間/キャッシュ読込)に対する単価の対応表。設定ファイルで管理する。
_Avoid_: 料金表、プライスリスト

**不明モデル (Unknown model)**:
単価テーブルに単価が定義されていないモデル名。使用量の集計には含めるが、金額は算出できないため「不明」として扱う。
_Avoid_: 未対応モデル

**為替レート (Exchange rate)**:
コストをJPY換算する際に使うUSD→JPYのレート。設定ファイルで管理し、算出対象の期間(本日/今月/先月/先々月)すべてに一律で適用する。
_Avoid_: 換算率

**コスト (Cost)**:
使用量に単価テーブルを適用して算出した金額。USDとJPYを併記する。
_Avoid_: 料金、課金額

**期間 (Period)**:
コストを集計する際の固定の区分。本日・今月・先月・先々月の4種類があり、実行環境のローカルタイムゾーンを基準に境界を判定する。
_Avoid_: 区間、レンジ
