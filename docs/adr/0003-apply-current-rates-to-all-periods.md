# 単価・為替レートは実行時点の設定値を全期間に一律適用する

本日/今月/先月/先々月のコストはすべて、実行時点の設定ファイルに書かれている単価テーブル・為替レートで算出する。過去の単価改定や為替変動の履歴は保持・追跡しない。これにより、設定ファイルを更新すると過去の期間の表示金額も新しい値で再計算される(先月・先々月の表示額が、更新前後で変わりうる)。正確な過去レートを追うための履歴管理は、個人のコスト把握という目的に対して複雑さが見合わないため見送った。
