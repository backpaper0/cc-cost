# サイドチェーン呼び出しをコスト集計に含める

サブエージェント呼び出しなどで発生する `isSidechain: true` のAPI呼び出しも、通常の呼び出しと同様にコスト集計へ含める。サイドチェーンは「裏側の処理だから請求とは別」という誤解をされやすいが、実際にはAnthropicへの課金が発生する呼び出しであり、除外すると算出コストが実際の請求額より小さく見積もられてしまう。集計時にサイドチェーンだけを特別扱いして除外・按分することはしない。
