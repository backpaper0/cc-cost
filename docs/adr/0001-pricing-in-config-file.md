# 単価テーブルと為替レートは設定ファイルで手動管理する

単価テーブル(モデルごとの入出力/キャッシュ単価)と為替レートは、Anthropicの価格ページやAPIから動的取得せず、ユーザーが編集する設定ファイル(`config.toml`)に静的な値として持たせる。個人利用のコスト把握ツールにおいて、外部依存や取得失敗時のフォールバックを増やすコストが、単価改定時に手動で設定ファイルを更新する手間を上回ると判断した。単価や為替レートが変わった場合はユーザーが値を書き換える運用とする。
